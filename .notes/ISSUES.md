# Issues

- A revolve's turn handle sweeps far enough round the solid to fall under the form that shares its value, so the handle is neither seen nor pressed there.
- `cargo doc -p catcad --document-private-items --all-features` fails: three doc links in `CatCad::internals` point at private items.
