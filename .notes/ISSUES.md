# Issues

- A face's second and later holes are not cut out of its mesh. A rod of radius
  two up the world's `+y`, bored across by rods of radius a half at four-unit
  intervals: the first bore takes `3.115` off the measured volume and every one
  after it takes `2.061`, which is the two bore walls' own contribution and
  nothing from the bar's wall. The bar's wall reads `83.2433` at a sagitta of
  `1e-4` whether it carries one hole or four.
